# Layout Lab 프로젝트 구현 가이드

이 문서는 Claude Code가 이 프로젝트와 동일한 수준의 업무용 웹 레이아웃 에디터를 구현하거나 유지보수할 때 따라야 할 지침이다.

## 공통 작업 원칙

- 시니어 프론트엔드 개발자와 UX 엔지니어의 관점에서 작업한다.
- 작업 전에 현재 프로젝트 구조, 패키지 관리자, 실행 스크립트와 변경 상태를 확인한다.
- 기존 구조와 사용자 변경사항을 보존하고 요청 범위 밖의 대규모 재구성은 하지 않는다.
- React와 TypeScript를 사용하며 타입 오류를 남기지 않는다.
- 현재 프로젝트의 패키지 관리자와 잠금 파일을 유지한다.
- 별도 서버가 필요한 기능을 임의로 추가하지 않는다.
- 편집 데이터와 사용자 설정은 브라우저 `localStorage`에 저장한다.
- 모든 ID는 충돌 가능성이 낮은 고유 값으로 생성한다.
- 신규 데이터 필드는 기존 저장 데이터에 없어도 정상 동작하도록 기본값을 제공한다.
- 외부 UI 프레임워크에 과도하게 의존하지 않는다.
- 구현이 끝나면 프로덕션 빌드를 실행하고 실제 오류를 모두 수정한다.
- 완료 보고에는 변경 내용, 검증 결과, 남은 제한사항만 간결하게 포함한다.

---

## 1단계: 프로젝트 분석 및 실행 환경 구성

현재 작업 폴더를 분석하고 업무용 웹 서비스의 메뉴와 페이지 레이아웃을 설계하는 웹 에디터의 기반을 구성한다.

요구사항:

- 기존 React 프로젝트가 있다면 구조와 패키지 관리자를 유지한다.
- 신규 프로젝트라면 Next.js 또는 현재 환경에 적합한 React 구조를 사용한다.
- 페이지, 요소, 테마, 저장 레이아웃을 명확한 TypeScript 타입으로 정의한다.
- 이후 기능을 한 페이지에서 확장할 수 있는 상태 관리 구조를 만든다.
- 개발 서버와 프로덕션 빌드가 모두 성공해야 한다.

완료 조건:

1. 개발 서버가 정상 실행된다.
2. TypeScript 오류가 없다.
3. 프로덕션 빌드가 성공한다.
4. 이후 기능을 추가할 수 있는 데이터 구조가 준비된다.

---

## 2단계: 다크 모던 편집기 화면 구성

관리자 대시보드 스타일의 레이아웃 에디터 셸을 구현한다.

참고 이미지:

https://elements-resized.envatousercontent.com/elements-cover-images/3aa43389-677d-4f67-971c-0c848730e02e?w=2038&cf_fit=scale-down&q=85&format=auto&s=82f86efb74111b30da4a242e6ac2e7a1f79372f8adcfcc4921308d19b2cf220f

화면 구성:

- 상단바
  - 제품 로고와 서비스명
  - 프로젝트 이름
  - 로컬 저장 상태
  - 미리보기
  - 불러오기
  - 저장
  - 테마 선택
- 좌측 페이지 메뉴
- 좌측 요소 도구상자
- 중앙 페이지 캔버스

디자인 기준:

- 배경, 사이드바, 패널, 카드와 팝오버의 레이어 대비를 명확히 한다.
- 카드 중심의 현대적인 관리자 대시보드 형태를 유지한다.
- 작은 보조 텍스트도 충분한 명암비를 확보한다.
- 테두리, 라운드, 그림자와 여백 규칙을 일관되게 사용한다.
- 메뉴와 도구상자가 화면 높이를 넘으면 각 영역 내부에서 스크롤한다.
- 업무용 데스크톱 환경을 우선 지원한다.
- 예시 데이터는 실제 업무 시스템처럼 구체적으로 작성한다.

---

## 3단계: 페이지 및 하위 페이지 구조

페이지 데이터에는 다음 필드를 포함한다.

- 고유 ID
- 페이지 이름
- 아이콘
- 상위 페이지 ID
- 해당 페이지에 포함된 요소 목록

기능 요구사항:

- 최상위 페이지를 추가할 수 있다.
- 각 페이지의 `＋` 버튼으로 하위 페이지를 추가할 수 있다.
- 하위 페이지에서도 다시 하위 페이지를 추가할 수 있다.
- 페이지 계층은 트리 순서로 정렬한다.
- 하위 페이지는 들여쓰기와 연결선으로 구분한다.
- 페이지를 선택하면 해당 페이지의 독립적인 요소 목록을 표시한다.
- 캔버스 상단에 현재 페이지의 상위 경로를 표시한다.
- 부모 ID가 없는 기존 데이터는 최상위 페이지로 처리한다.
- 잘못된 부모 ID나 순환 참조가 있어도 무한 반복되지 않게 방어한다.
- 페이지가 많아지면 페이지 메뉴만 스크롤한다.
- 페이지 이름은 캔버스 상단에서 바로 수정할 수 있다.

---

## 4단계: 요소 도구상자

도구상자는 다음 카테고리로 구성한다.

### 기본 구성

- 페이지 헤더
- 텍스트 블록
- 액션 버튼
- 입력 폼

### 정보 카드

- 핵심 지표 카드
- 서비스 상태 카드
- 진행률 카드
- 사용자 프로필 카드

### 인터랙티브 차트

- 추세 차트
- 막대 차트
- 도넛 차트
- 게이지 차트

### 업무 도구

- 업무 게시판
- 콘텐츠 에디터
- 실시간 활동 피드
- 담당자 배정
- 투표

도구 동작:

- 도구를 클릭하면 현재 페이지에 요소를 추가한다.
- 도구를 캔버스로 드래그해도 추가할 수 있다.
- 키보드와 터치 환경을 위해 클릭 추가 기능을 항상 유지한다.
- 새 요소에는 고유 ID를 부여한다.
- 추가 직후 해당 요소의 설정창을 연다.
- 빈 페이지에는 요소 배치 방법을 안내한다.
- 각 도구에는 아이콘, 이름과 한 줄 설명을 표시한다.

---

## 5단계: 요소 순서 변경

캔버스의 요소는 전용 `⠿` 이동 핸들로 순서를 변경한다.

구현 기준:

- 요소 전체에 브라우저 기본 `draggable`만 적용하지 않는다.
- 이동 핸들에서 포인터 캡처를 시작한다.
- 마우스와 터치 포인터를 모두 지원한다.
- 포인터 아래의 대상 카드와 앞·뒤 삽입 위치를 계산한다.
- 드래그 중인 카드는 반투명하게 표시한다.
- 삽입 대상은 포인트 컬러로 강조한다.
- 캔버스 하단에 놓으면 마지막 순서로 이동한다.
- 카드 내부 버튼, 입력 필드, 차트와 에디터 조작은 드래그와 충돌하지 않아야 한다.
- 드래그 완료 또는 취소 시 포인터 캡처와 임시 상태를 안전하게 해제한다.
- 순서 변경 후에도 요소 ID, 설정과 크기를 유지한다.

---

## 6단계: 요소별 플로팅 설정

각 요소 오른쪽 위의 설정 버튼으로 플로팅 설정창을 연다.

공통 설정:

- 제목 수정
- 가로 크기: `1/3`, `1/2`, `전체`
- 세로 크기: `자동`, `S`, `M`, `L`, `XL`
- 160~600px 세부 높이 슬라이더
- 요소 복제
- 요소 삭제

세로 크기 처리:

- 자동은 콘텐츠 높이를 따른다.
- 숫자 높이를 선택하면 카드 높이를 고정한다.
- 콘텐츠가 카드보다 길면 카드 콘텐츠 영역만 스크롤한다.
- 플로팅 설정창은 카드 경계에 잘리지 않아야 한다.
- 저장 및 복제 시 세로 크기도 유지한다.

요소별 설정:

- 페이지 헤더: 라벨, 제목, 설명
- 텍스트: 본문
- 버튼: 버튼 문구
- 지표 카드: 값, 증감률, 보조 설명
- 진행률, 도넛, 게이지: 표시 수치
- 프로필: 이름과 역할
- 차트: 제목과 보조 설명
- 투표: 질문과 3개 선택지
- 나머지 요소: 제목과 보조 설명

설정창 내부 조작은 요소 이동과 충돌하지 않아야 하며 외부를 클릭하면 닫힌다.

---

## 7단계: 요소 인터랙션

요소는 정적인 이미지가 아니라 실제로 조작 가능해야 한다.

### 차트

- 오늘, 7일, 30일 기간 선택
- 기간에 따른 데이터 변경
- 막대나 데이터에 마우스를 올리면 값 표시
- 현재 테마의 포인트 컬러 사용

### 업무 게시판

- 전체, 진행 중, 검토, 완료 필터
- 업무명, 담당자, 우선순위, 상태 표시
- 상태 버튼을 누르면 다음 상태로 변경

### 콘텐츠 에디터

- 제목과 본문 직접 편집
- 굵게, 기울임, 제목, 목록 등의 기본 툴바

### 실시간 피드

- 현재 시각을 초 단위로 갱신
- LIVE 상태와 최근 활동 목록 표시
- 컴포넌트 해제 시 타이머 정리

### 담당자 배정

- 담당자 선택
- 업무 완료 체크박스
- 마감 상태 표시

### 투표

- 질문과 3개 선택지
- 단일 선택 후 투표
- 투표 후 선택지별 비율과 참여 인원 표시
- 다시 선택 기능
- 익명 투표 안내

---

## 8단계: 로컬 저장 및 불러오기

다운로드나 업로드 없이 브라우저 `localStorage`에 저장한다.

저장 데이터:

- 레이아웃 이름
- 저장 시각
- 선택된 테마 ID
- 전체 페이지 목록
- 페이지의 부모·자식 관계
- 페이지별 요소 목록과 순서
- 요소 설정값
- 요소 가로 및 세로 크기

기능 요구사항:

- 상단 프로젝트 이름을 저장 이름으로 사용한다.
- 같은 이름으로 저장하면 기존 데이터를 갱신한다.
- 저장된 레이아웃 목록에서 원하는 항목을 불러올 수 있다.
- 저장 이름과 시간을 표시한다.
- 저장 항목을 삭제할 수 있다.
- 현재 편집 상태는 별도의 자동 임시 저장 데이터로 보존한다.
- 새로고침 시 임시 저장 데이터를 복원한다.
- 손상된 JSON은 무시하고 기본 레이아웃으로 안전하게 시작한다.
- 저장과 불러오기 완료 시 토스트를 표시한다.
- 파일 다운로드와 업로드 기능은 만들지 않는다.

---

## 9단계: 테마 30종

CSS 변수 기반 테마 시스템을 구현한다.

테마 필드:

- 전체 배경
- 사이드바
- 패널
- 카드 표면
- 상승된 레이어
- 테두리
- 기본 텍스트
- 보조 텍스트
- 주 포인트 컬러
- 보조 포인트 컬러
- 성공 컬러

총 구성:

- 모던 다크 테마 20종
- 라이트 테마 10종

다크 테마 예시:

- Violet Night
- Ocean Dark
- Rose Charcoal
- Cobalt Ink
- Obsidian Pulse
- Carbon Cyan
- Midnight Electric
- Graphite Ruby
- Black Gold
- Neo Indigo
- Forest Matrix
- Velvet Plum
- Steel Orange
- Polar Night

테마 요구사항:

- 배경색뿐 아니라 모든 레이어 색상을 함께 변경한다.
- 카드, 배경, 패널과 사이드바 사이에 충분한 대비를 둔다.
- 보조 텍스트도 읽을 수 있는 명암비를 유지한다.
- 신규 다크 테마 10종에는 `NEW`를 표시한다.
- 테마 버튼에는 실제 테마 개수를 동적으로 표시한다.
- 각 테마의 색상 미리보기를 제공한다.
- 테마 목록이 화면보다 길면 팝오버 내부만 스크롤한다.
- 선택한 테마 ID를 저장 데이터에 포함한다.

---

## 10단계: 미리보기 및 접근성

미리보기 모드에서 숨길 항목:

- 도구상자
- 설정 버튼
- 이동 핸들
- 페이지 추가 버튼
- 하위 페이지 추가 버튼
- 드래그 안내
- 저장 및 불러오기 같은 편집 전용 UI

유지할 항목:

- 페이지 사이드바와 계층
- 현재 페이지 콘텐츠
- 요소 인터랙션
- 적용된 테마

접근성 기준:

- 모든 주요 버튼에 `aria-label`을 제공한다.
- 키보드 포커스를 명확히 표시한다.
- 비활성 상태는 색상, 투명도와 커서로 구분한다.
- 팝오버가 동시에 여러 개 열리지 않게 한다.
- 편집 모드로 돌아오면 기존 페이지와 설정을 유지한다.

---

## 11단계: 통합 검증

다음 기능을 순서대로 검증한다.

1. 최상위 페이지 추가
2. 다단계 하위 페이지 추가
3. 페이지별 독립 요소 관리
4. 도구 클릭 추가
5. 도구 드래그 추가
6. 이동 핸들로 요소 순서 변경
7. 요소 설정 수정
8. 가로 크기 변경
9. 세로 크기 프리셋과 슬라이더 변경
10. 요소 복제와 삭제
11. 차트 기간 선택
12. 게시판 필터 및 상태 변경
13. 담당자 선택
14. 투표 및 결과 확인
15. 실시간 시계 갱신
16. 콘텐츠 편집
17. 테마 30종 전환
18. 이름 기반 저장과 불러오기
19. 새로고침 후 임시 저장 복원
20. 미리보기 전환

추가 확인:

- TypeScript 오류가 없어야 한다.
- 프로덕션 빌드가 성공해야 한다.
- 반복적인 콘솔 오류가 없어야 한다.
- 타이머와 이벤트 리스너 누수가 없어야 한다.
- 기존 저장 데이터에 신규 필드가 없어도 동작해야 한다.
- 요소 내부 조작과 드래그가 충돌하지 않아야 한다.
- 고정 높이에서 콘텐츠가 카드 외부로 넘치지 않아야 한다.
- 손상된 `localStorage` 데이터가 앱 전체를 중단시키지 않아야 한다.

실패한 기능은 원인을 찾아 수정하고 다시 검증한다. 기능 삭제나 임시 우회 코드로 검증을 통과시키지 않는다.

---

## 12단계: 최종 정리 및 전달

- 사용하지 않는 스타터 코드와 의존성을 제거한다.
- 프로젝트 제목과 설명을 실제 서비스 이름으로 변경한다.
- README에 서비스 개요, 주요 기능, 설치, 개발 서버, 빌드, 저장 방식과 데이터 구조를 작성한다.
- 비밀키, 로컬 환경 파일과 임시 파일을 포함하지 않는다.
- 압축 파일에서 `node_modules`, 빌드 결과, 캐시와 Git 내부 이력을 제외한다.
- 현재 검증된 소스만 ZIP으로 생성한다.
- 최종 프로덕션 빌드를 한 번 더 실행한다.

최종 보고에는 다음 내용을 포함한다.

- 구현된 주요 기능
- 검증 및 빌드 결과
- 압축 파일 위치와 크기
- 알려진 제한사항

